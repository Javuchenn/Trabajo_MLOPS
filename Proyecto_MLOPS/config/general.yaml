
training:
  epochs: 100
  batch_size: 32
  shuffle: true

early_stopping:
  patience: 10
  monitor: val_loss

model:
  path: models/best_model.h5

data:
  path: Data/features_30_sec.csv

features:
  input_dim: 58
  num_classes: 10